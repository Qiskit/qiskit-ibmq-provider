---
features:
  - |
    Add support for the ``use_measure_esp`` flag in the
    :meth:`qiskit.providers.ibmq.IBMQBackend.run` method. If ``True``, the final measurement in each
    circuit will use excited state promoted (ESP) readout.
